server:
  port: 9092

spring:
  application:
    name: hello-server

management:
  endpoints:
    web:
      exposure:
        include: "*"

# TODO fix interface 导致 不可用的接口被注册

feign:
  hystrix:
    enabled: true